Run `ansible-galaxy init` for each role you want to create.
ansible-galaxy init role1
ansible-galaxy init role2
Replace `role1`, `role2`, etc., with your actual role names.
ansible-galaxy init spanning-tree
